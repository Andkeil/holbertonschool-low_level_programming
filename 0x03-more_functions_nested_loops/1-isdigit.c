#include "holberton.h"

/**
 * _islower - check the code for Holberton School students.
 * @i: number checked if it's digit
 *
 * Return: 1 or 0.
 */

int _isdigit(int c)
{
	if (c > 47 && c <= 57)
	{
		return (1);
	}
	return (0);
}
