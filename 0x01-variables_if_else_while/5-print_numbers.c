#include <stdio.h>

/**
 * main - Entry point
 *
 * purpose - print single digit base10 numbers
 *
 * Return: (0) always
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
